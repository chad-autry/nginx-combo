---
dependencies:
  - role: discovery
    when: not proxy_rethinkdb
    vars:
      parent: 'route_discovery'
      service: 'rethinkdb'
      port: "{{ports['rethinkdb_admin']}}"
      service_properties:
        strip: 'true'
        private: 'true'
